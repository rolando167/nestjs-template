const USER_ADDED = 'USER_ADDED';
//=================================================
const SCHEMA_DB = 'public';

//=================================================
const DATA_BASE_MAIN = "postgres" || "mocktest";
//=================================================

export {
    USER_ADDED,
    SCHEMA_DB,
    DATA_BASE_MAIN
}